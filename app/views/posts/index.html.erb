<p id="notice"><%= notice %></p>

<h1>All Posts</h1>

<table>
    <thead>
        <tr>
            <th>Posts</th>
        </tr>
    </thead>

    <tbody>
        <% @posts.each do |post| %>
        <tr>

         <% if user_signed_in? %>
         <td><%= post.posts %></td>
        <% end %>
        <% end %>
            
    </tbody>
</table>
<!--
<div>
        <% @posts.each do |post| %>
         <% if user_signed_in? %>
         <%= post.posts %>
        <% end %>
        <% end %>
    
</div>
-->


<%= link_to 'Create New Post', new_post_path %>
